import { defineService } from '@profullstack/sh1pt-core';

export default defineService({
  id: 'service-proxyscotch',
  label: "Proxyscotch",
  category: "dev",
  description: "CORS proxy server",
  coolifyTemplate: "proxyscotch",
  homepageUrl: 'https://coolify.io/services',
});
