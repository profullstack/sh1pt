# Nextcloud With Postgres

File storage with PostgreSQL.

## What it does

- Registers a deployable service in the sh1pt service catalog.
- Exposes service metadata used by deployment and catalog tooling.

## Package

- Name: `@profullstack/sh1pt-service-nextcloud-with-postgres`
- Path: `services/nextcloud-with-postgres`
- Adapter ID: `service-nextcloud-with-postgres`
- Category: `files`
- Homepage: https://sh1pt.com

## Scripts

- `build`: `tsc -p tsconfig.json`
- `prepublishOnly`: `pnpm build`
- `typecheck`: `tsc -p tsconfig.json --noEmit`

## Usage

```bash
pnpm add @profullstack/sh1pt-service-nextcloud-with-postgres
```

## Development

```bash
pnpm --filter @profullstack/sh1pt-service-nextcloud-with-postgres typecheck
```

Run tests from the repository root when this module includes a test file:

```bash
pnpm vitest run services/nextcloud-with-postgres/src/index.test.ts
```

<!-- Generated by scripts/gen-module-readmes.mjs -->
