# xAI Agent Provider

OpenAI-compatible sh1pt agent provider for xAI.

Required env:

- `XAI_API_KEY`

Optional env:

- `XAI_BASE_URL` defaults to `https://api.x.ai/v1`
- `XAI_MODEL` defaults to `grok-4.3`
