# @sh1pt/web

Dashboard UI — stub. Pick a framework (Next.js, SvelteKit, Remix) and wire it to `@sh1pt/sdk` against `@sh1pt/api`.

Screens to implement:

- Project overview (targets matrix, latest release per target, live/rolled-back state)
- Release detail (per-target build logs, artifact, ship result, store URL)
- Credentials vault (add/edit/delete; values never returned)
- Webhooks (subscriptions + inbound event log)
- Team / billing
