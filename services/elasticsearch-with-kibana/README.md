# Elasticsearch With Kibana

Search, monitoring, and visualization.

## What it does

- Registers a deployable service in the sh1pt service catalog.
- Exposes service metadata used by deployment and catalog tooling.

## Package

- Name: `@profullstack/sh1pt-service-elasticsearch-with-kibana`
- Path: `services/elasticsearch-with-kibana`
- Adapter ID: `service-elasticsearch-with-kibana`
- Category: `search`
- Homepage: https://sh1pt.com

## Scripts

- `build`: `tsc -p tsconfig.json`
- `prepublishOnly`: `pnpm build`
- `typecheck`: `tsc -p tsconfig.json --noEmit`

## Usage

```bash
pnpm add @profullstack/sh1pt-service-elasticsearch-with-kibana
```

## Development

```bash
pnpm --filter @profullstack/sh1pt-service-elasticsearch-with-kibana typecheck
```

Run tests from the repository root when this module includes a test file:

```bash
pnpm vitest run services/elasticsearch-with-kibana/src/index.test.ts
```

<!-- Generated by scripts/gen-module-readmes.mjs -->
